<Window x:Class="Todowork.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Todowork" Height="520" Width="420"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/TodoworkTD_gray.ico"
        WindowStyle="None"
        ResizeMode="CanResize"
        PreviewKeyDown="Window_PreviewKeyDown"
        Closing="MainWindow_Closing">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="34" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Grid Background="{StaticResource BrushSurface}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorderSubtle}" BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid Height="34">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Todowork" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{StaticResource BrushTextPrimary}"
                           FontFamily="{StaticResource AppFontFamily}" FontSize="{StaticResource FontSizeTitle}" />

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Stretch">
                    <Button Style="{StaticResource TitleBarButton}" ToolTip="设置"
                            Click="OpenSettings_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14" />
                    </Button>
                    <Button Content="✕" Style="{StaticResource TitleBarButton}" Foreground="{StaticResource BrushDanger}"
                            Click="Close_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="{StaticResource AppPadding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="待办" Margin="0,0,6,0"
                        Command="{Binding ShowTodoCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource TabButton}">
                            <Setter Property="Background" Value="{StaticResource BrushAccentLight}" />
                            <Setter Property="BorderBrush" Value="{StaticResource BrushAccent}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowCompleted}" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BrushSurfaceAlt}" />
                                    <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Grid.Column="1" Content="已完成" Margin="6,0,0,0"
                        Command="{Binding ShowCompletedCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource TabButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowCompleted}" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BrushAccentLight}" />
                                    <Setter Property="BorderBrush" Value="{StaticResource BrushAccent}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="2" Content="清空" Margin="12,0,0,0"
                        Command="{Binding ClearCompletedCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowCompleted}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ItemsView.IsEmpty}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <Grid Grid.Row="1">
                <ListView ItemsSource="{Binding ItemsView}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" Margin="0,0,0,10" HorizontalContentAlignment="Stretch"
                      Focusable="False" IsTabStop="False" FocusVisualStyle="{x:Null}"
                      KeyboardNavigation.TabNavigation="None" KeyboardNavigation.DirectionalNavigation="None"
                      IsSynchronizedWithCurrentItem="False"
                      BorderThickness="0" BorderBrush="Transparent" Background="Transparent">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Focusable" Value="False" />
                            <Setter Property="OverridesDefaultStyle" Value="True" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                SnapsToDevicePixels="True">
                                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                </Trigger>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                                    CornerRadius="{StaticResource RadiusM}" Padding="8,6" Margin="0,4" MinHeight="40">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding PlacementTarget.DataContext.TogglePinCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding}">
                                            <MenuItem.Style>
                                                <Style TargetType="MenuItem">
                                                    <Setter Property="Header" Value="置顶" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                            <Setter Property="Header" Value="取消置顶" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </MenuItem.Style>
                                        </MenuItem>
                                        <MenuItem Header="删除"
                                                  Command="{Binding PlacementTarget.DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                  CommandParameter="{Binding}" />
                                    </ContextMenu>
                                </Border.ContextMenu>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="70" />
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" VerticalAlignment="Center" IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                              Focusable="False" IsTabStop="False" FocusVisualStyle="{x:Null}" />

                                    <Grid Grid.Column="1" Margin="8,0,8,0" VerticalAlignment="Center">
                                        <TextBlock x:Name="ViewText" Text="{Binding Text}" TextWrapping="Wrap"
                                                   MouseLeftButtonDown="TodoText_MouseLeftButtonDown" />
                                        <TextBox x:Name="EditText" Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap"
                                                 Visibility="Collapsed" BorderBrush="{StaticResource BrushAccent}" BorderThickness="1" Background="{StaticResource BrushSurface}"
                                                 Padding="4,2" Loaded="EditTextBox_Loaded" PreviewKeyDown="EditTextBox_PreviewKeyDown"
                                                 LostFocus="EditTextBox_LostFocus" />
                                    </Grid>

                                    <StackPanel x:Name="ActionPanel" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right"
                                                Opacity="0" IsHitTestVisible="False">
                                        <Button ToolTip="置顶" Margin="0,0,4,0"
                                                Command="{Binding DataContext.TogglePinCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}" Focusable="False" IsTabStop="False" FocusVisualStyle="{x:Null}">
                                            <TextBlock Text="&#xE718;" />
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource ItemIconButton}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource BrushAccent}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <Button Style="{StaticResource ItemIconButton}" ToolTip="删除"
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}" Focusable="False" IsTabStop="False" FocusVisualStyle="{x:Null}">
                                            <TextBlock Text="&#xE74D;" />
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                    <Setter TargetName="ViewText" Property="Visibility" Value="Collapsed" />
                                    <Setter TargetName="EditText" Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsMouseOver}" Value="True">
                                    <Setter TargetName="ActionPanel" Property="Opacity" Value="1" />
                                    <Setter TargetName="ActionPanel" Property="IsHitTestVisible" Value="True" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource BrushTextSecondary}" FontSize="{StaticResource FontSizeBody}"
                           Visibility="{Binding ItemsView.IsEmpty, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="暂无待办，输入后回车添加" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowCompleted}" Value="True">
                                    <Setter Property="Text" Value="暂无已完成事项" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <TextBox Grid.Row="2" Height="34" VerticalContentAlignment="Center"
                 Text="{Binding NewText, UpdateSourceTrigger=PropertyChanged}"
                 Tag="输入待办事项，回车键确认"
                 Style="{StaticResource WatermarkTextBox}"
                 BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Background="{StaticResource BrushSurface}" Padding="6,0,6,0"
                 PreviewKeyDown="NewTextBox_PreviewKeyDown" />
        </Grid>
    </Grid>
</Window>
