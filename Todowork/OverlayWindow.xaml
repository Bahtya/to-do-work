<Window x:Class="Todowork.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overlay" Width="320" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Loaded="OverlayWindow_Loaded"
        SizeChanged="OverlayWindow_SizeChanged"
        Closing="OverlayWindow_Closing"
        >
    <Grid>
        <Border x:Name="BgBorder" Background="#111111"
                Opacity="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=BackgroundOpacity}"
                CornerRadius="10">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=ShowBackground}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>

        <Grid x:Name="ContentGrid" Margin="10"
              Opacity="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=TextOpacity}"
              TextElement.Foreground="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=TextBrush}"
              TextElement.FontSize="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=TextFontSize}">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Margin" Value="10" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=ShowBackground}" Value="False">
                            <Setter Property="Margin" Value="0" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="待办事项" FontWeight="Bold"
                       Foreground="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=TextBrush}" />

            <ListBox Grid.Row="1" ItemsSource="{Binding PinnedView}" Background="Transparent" BorderThickness="0" Margin="0,8,0,0"
                    Focusable="False" IsTabStop="False" FocusVisualStyle="{x:Null}"
                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                    ScrollViewer.HorizontalScrollBarVisibility="Hidden">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Focusable" Value="False" />
                        <Setter Property="IsTabStop" Value="False" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderBrush" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0" SnapsToDevicePixels="True">
                                        <ContentPresenter SnapsToDevicePixels="True"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <TextBlock Text="⏹️" FontWeight="Bold" Margin="0,0,6,0"
                                       Foreground="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=TextBrush}" />
                            <TextBlock FontWeight="Bold" Text="{Binding Text}" TextWrapping="Wrap"
                                       Foreground="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=TextBrush}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Window>
